venv/
__pycache__/
*.pyc

# A base bruta não é necessária para o app (ele lê só os CSVs tratados).
# Remova esta linha se quiser versionar também a entrada do tratamento.
vendas.xlsx
atendimento.xlsx
clientes.xlsx
estoque.xlsx

# Cadastro de clientes tratado: não é usado pelo app (não reconcilia com vendas)
clientes_tratado.csv

# Credenciais da API do Elo Agents (nunca versionar)
.streamlit/secrets.toml
.env
